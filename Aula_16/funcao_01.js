function parimp(n) { // parimp(n) paraêmetro 
    if (n % 2 == 0) {
        return 'Par' // Possível retorno
    } else {
        return 'Impar' // Possível retorno
    }
}
/*
let res = parimp(29) // chamada
console.log(res) // Para aparecer na tela 
*/
console.log(parimp(233)) // Maneira mais simples de criar uma chamada e faze-lá aparecer na tela